package cpp;

/**
 * An alternative way to use `double` in the compiled C++ code.
 * `Float64` compiles to double, however, you may perfer to use a `Double` type instead.
 */
typedef Double = Float64;